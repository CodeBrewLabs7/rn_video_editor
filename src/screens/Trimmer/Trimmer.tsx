import React from 'react';
import { Text } from 'react-native';
import WrapperContainer from '../../components/WrapperContainer';

const Trimmer = (): React.JSX.Element => {
  return (
    <WrapperContainer>
      <Text>Editor</Text>
    </WrapperContainer>
  );
};

export default Trimmer;
